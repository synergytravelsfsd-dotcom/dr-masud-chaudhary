# Internationalization readiness

Locales scaffolded in `src/lib/i18n.ts`:

- `en` (default)
- `ur`
- `pt`
- `fr`
- `ar`

## Recommended next step

Adopt `next-intl` with `[locale]` segment:

```text
src/app/[locale]/(marketing)/...
```

Keep content source-of-truth in Supabase with per-locale fields (`title_en`, `title_ur`, …) or JSONB `translations`.
